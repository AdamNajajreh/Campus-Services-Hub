import React from "react";
import { TopBar } from "../Common/TopBar";

/**
 * @component
 * @description AdminRequests component
 * @returns The Admin Requests page layout with top bar and content
 */
export const AdminRequests = () => {
  return (
    <div className="bg-white rounded-lg pb-4 shadow">
      <TopBar />
      {/* Admin requests management content will be later */}
    </div>
  );
};
